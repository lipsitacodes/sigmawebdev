Wite a program using prompt function to take age as a value from the user and use alert function to tell him if he can drive or not
In 21 Mal Confirm to ask the user if he wantsto see the prompt again
In the previous question use Consle esror to log the error! if the age entered is negative
Write a program to change the wit to gogle. com(Redieection) f wser enters a number grester than 4
Change the backeround of the page to yellos redor any other Color based on user input through prompt
