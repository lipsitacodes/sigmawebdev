create an array of numbers and take input from the user to add numbers to the array 
keep adding number to the array in above question untill 0 is added to it
filter for numbers divisible by 10 from  Given array
Create an aray of squsre of given numbers
Use reduce to calculate factorial of a given
number from an array of first n natural numbers (n being the nunber whose factorial needs to be calculated)